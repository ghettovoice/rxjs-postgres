branches:
  only:
    - master
language: node_js
node_js:
  - 6
addons:
  postgresql: 9.4
cache:
  directories:
    - node_modules
before_script:
  - createdb -U postgres test
  - psql -U postgres -d test -f ./tests/test.sql
script: npm run test:cover && npm run test:coveralls
